.git/
.gitignore
/*.md
docs/
compose.yaml

# seed/ is not needed in the build context: pi's config home (~/.pi) is
# bind-mounted live at runtime (seed/.pi -> /pi-agent/.pi), never COPY'd
# into the image.
seed/
