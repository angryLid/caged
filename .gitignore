# Node / npm runtime state that pi may drop into the working tree
node_modules/
.pnpm-store/

# OS cruft
.DS_Store

# Shell / editor leftovers
*.swp
.idea/
.vscode/

# Runtime state pi writes back into the live seed bind mount
seed/agent/auth.json
seed/agent/sessions/
