---
name: skills-sync
description: Sync the project's declaratively-configured skills (skills.json) — clone/pull the source skill repos into vendor/skills/ and (re)create the symlinks that make them available to pi (and other agents). Use when the user says "sync skills", "update skills", "pull skills", "install skills", "refresh skills", after editing skills.json, or when newly added skills aren't showing up.
---

# skills-sync

Synchronises the skills declared in the project's `skills.json` into the
locations the agent scans for skills.

## What it does

1. Reads `skills.json` (declarative config at the project root).
2. For each `repos` entry: **clone** the repo into `vendor/skills/<name>` if missing,
   otherwise **`git pull`** it to latest.
3. For each enabled skill, creates a **relative symlink** inside each
   `linkTargets` dir (default `.pi/skills`) pointing back into `vendor/skills/`.
4. Removes **stale symlinks** this tool previously created (targets resolving
   into `vendor/skills/`), so skills removed from config stop loading.

## How to run

```bash
node scripts/skills-sync.mjs          # do it
node scripts/skills-sync.mjs --dry-run   # preview without changing anything
```

Exit codes: `0` ok, `1` fatal, `2` warnings only (missing skill / collision) — links still applied.

## When the user edits `skills.json`

- Adding a skill to an existing repo's `enabled` → just re-run the script.
- Adding a whole new repo → the script clones it on the next run.
- Removing a skill → the script unlinks its symlink on the next run.

## Notes / gotchas

- **`vendor/skills/` and `.pi/skills/` are gitignored** and regenerated by this
  script — don't commit them, don't edit symlinks by hand.
- **Collisions**: two repos can ship a skill with the same name (e.g. `handoff`
  exists in both gitlab-ai-skills and mattpocock-skills). pi keeps the first
  one loaded and warns. Enable only one of a colliding pair.
- **Relative symlinks** are used on purpose so nothing is hardcoded to an
  absolute path — the links survive the project being cloned/mounted elsewhere.
- Repos are plain clones (not submodules) — the declarative config `skills.json`
  is the source of truth, and a fresh clone self-heals by re-cloning.